## Packages
recharts | Data visualization for dashboard charts
framer-motion | Smooth animations for page transitions and components

## Notes
Dashboard data is fetched via /api/dashboard endpoints
Alerts management uses PATCH /api/alerts/:id/status
Theme uses warm orange/red gradients as requested
